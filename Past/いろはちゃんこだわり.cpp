/*int a,b;
  cin >> a >> b;
  putvec(vec,b)
  for (int i = a; i < 10*a; i++) {
        bool ok = true;
        string ch = to_string(i);
        int x = ch.size();
        rep(j,x) rep(l,b) {
            int dd = vec[l];
            char c = ch[j];
            if(vec[l] + '0' == ch[j]) ok = false;
        }
        if(ok) {
            cout << i << endl;
            return 0;
        }
    }*/